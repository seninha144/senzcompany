# AFTERHOURS media

- `afterhours-preview.mp4`: the supplied `images/video_afterhours.mp4`, copied unchanged (1920 × 1080, 24.9 seconds).
- `afterhours-cover.webp`: a real frame extracted at 3 seconds from that video, showing the intro, AFTERHOURS, JOIN and the barber/pixel composition. No generated imagery.

Replace these files here when updating the media. Rebuild after replacing or adding the cover, which is detected at build time. Public URLs omit `/public`.

For future previews, preserve the intro → JOIN → pixel UFO → landing → THE SPACE → THE CUT → THE BARBER sequence. The shared player loads the video when visible, pauses offscreen, respects reduced motion, and uses muted inline looping playback. Media uses `object-fit: contain` to preserve the full composition on every screen size.

This is a fictional concept by SENZ. No live URL or unverified technology tags are configured.
